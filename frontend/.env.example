VITE_API_BASE_URL=/api/v1
VITE_APP_NAME=Fulfillment Transit
# Для frontend-dev контейнера docker-compose переопределяет это значение на http://host.docker.internal:8080
# Для локального npm run dev можно не задавать:
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
